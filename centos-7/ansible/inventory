[all:vars]
packer_dir="{{ playbook_dir | dirname }}"
build_dir="{{ packer_dir }}/builds"
staging_dir="{{ build_dir }}/staging"

[source_webserver]
#w1.foo.org           ansible_user=mheiges

[buildhost]
localhost               ansible_connection=local